import { PageContainer } from '@/components/layout/PageContainer';
import { UnderConstruction } from '@/components/ui/UnderConstruction';

export const metadata = { title: '公告详情' };

export default function AnnouncementDetailPage() {
  return (
    <PageContainer>
      <UnderConstruction />
    </PageContainer>
  );
}
