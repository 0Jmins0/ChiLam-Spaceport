import { PageContainer } from '@/components/layout/PageContainer';
import { UnderConstruction } from '@/components/ui/UnderConstruction';

export const metadata = { title: '活动详情' };

export default function ActivityDetailPage() {
  return (
    <PageContainer>
      <UnderConstruction />
    </PageContainer>
  );
}
